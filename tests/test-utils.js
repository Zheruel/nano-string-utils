/**
 * Type testing utilities
 */

/**
 * Assert that a value has the expected type
 * This is a compile-time check - the function itself does nothing at runtime
 * @template T
 * @param {T} _value - The value to check (unused at runtime)
 */
export function expectType(_value) {
  // This function is only for type checking at compile time
  // The actual implementation does nothing
}